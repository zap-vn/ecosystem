using System; using Npgsql; var conn = new NpgsqlConnection("Host=136.118.121.105;Port=5432;Database=ecosystem_v110;Username=postgres;Password=Pg@Secret2026!;Include Error Detail=true;"); conn.Open(); using var cmd = new NpgsqlCommand("CREATE SCHEMA IF NOT EXISTS identity; CREATE TABLE IF NOT EXISTS identity.user (id UUID PRIMARY KEY, email VARCHAR(150), phone_number VARCHAR(50), password_hash VARCHAR(500) NOT NULL, full_name VARCHAR(200), avatar_id VARCHAR(500), status_id INTEGER NOT NULL DEFAULT 9001, tenant_id UUID, is_active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP WITH TIME ZONE);", conn); cmd.ExecuteNonQuery(); Console.WriteLine("Table created.");
