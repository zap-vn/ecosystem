using System;
using Scalar.AspNetCore;

public class Checker {
    public void Check() {
        var opts = new ScalarOptions();
        // Intentional build error to see available members if possible, or just guess
        // opts.
    }
}
