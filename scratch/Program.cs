using System; using Npgsql; using var conn = new NpgsqlConnection("Host=136.118.121.105;Port=5432;Database=ecosystem_v110;Username=postgres;Password=Pg@Secret2026!;Include Error Detail=true;"); conn.Open(); using var cmd = new NpgsqlCommand("SELECT table_schema, table_name FROM information_schema.tables WHERE table_schema IN ('identity', 'public') AND table_name LIKE '%user%';", conn); using var reader = cmd.ExecuteReader(); while (reader.Read()) Console.WriteLine($"{reader[0]}.{reader[1]}");
